# Login (NextAuth). En producción los usuarios viven en la tabla app_users.
# AUTH_USERNAME/AUTH_PASSWORD sirven como fallback y para crear el primer admin (npm run seed:admin).
AUTH_SECRET=cambia-esto-por-un-secreto-largo-y-aleatorio
AUTH_USERNAME=admin
AUTH_PASSWORD=Firox2026
AUTH_DISPLAY_NAME=Administrador

# Supabase (Settings → API en tu proyecto)
NEXT_PUBLIC_SUPABASE_URL=https://zeuktcfrfkaxlyauwjqf.supabase.co
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key
SUPABASE_STORAGE_BUCKET=product-images

# Opcional en local: si no configurás Supabase, usa archivos JSON locales
